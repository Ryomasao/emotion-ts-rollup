# emotion + React(typescript) + rollup

```
.
├── README.md
├── node_modules
├── package.json
├── packages 
├── sites // play ground created by CRA
└── yarn.lock
```

build

```sh
cd packages/ui
yarn build
```

playground

```
cd site
yarn start
```